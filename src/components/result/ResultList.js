import React from 'react';
import { ListGroup } from 'react-bootstrap';
import { Result } from '../';

const ResultList = () => (
    <ListGroup>
        <Result />
        <Result />
        <Result />
    </ListGroup>
);

export default ResultList;